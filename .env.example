# Base URL of the smilekrub-backend Elysia API (Better Auth lives at /api/auth).
# VITE_ prefix so the same value is readable from the browser and the Nitro server.
VITE_API_URL=http://localhost:3001
